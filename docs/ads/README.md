# AdMob banner integration

The free build reserves a banner area above the bottom navigation. During development, use Google's official test banner unit ID. Before production release, configure the real AdMob App ID and banner unit ID through release-only resources or the secure release configuration; never commit private credentials.

Google's current Android Mobile Ads SDK documentation requires an AdMob App ID in the application manifest and recommends test ads during development.
